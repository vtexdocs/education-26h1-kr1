# Issue: Update orders via Checkout API

| Field | Value |
|-------|--------|
| **issue_id** | checkout-api-orders-01 |
| **persona** | Developer |
| **product** | Checkout API / Orders |
| **user_intent** | How to update/modify orders via Checkout API. |
| **expected_doc_url** | https://developers.vtex.com/docs/api-reference/checkout-api#put-/api/checkout/pub/orders |
| **source** | Command input (target document URL) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how to send order data in vtex checkout api what format |
| familiar | checkout api update order request body format vtex |
| expert | checkout api put orders json payload structure vtex |

**Array (query_external):**
```json
[
  { "query": "how to send order data in vtex checkout api what format", "style": "naive" },
  { "query": "checkout api update order request body format vtex", "style": "familiar" },
  { "query": "checkout api put orders json payload structure vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | checkout api order request body format |
| familiar | put orders checkout json structure |
| expert | checkout api orders payload |

**Array (query_internal):**
```json
[
  { "query": "checkout api order request body format", "style": "naive" },
  { "query": "put orders checkout json structure", "style": "familiar" },
  { "query": "checkout api orders payload", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs checkout api update order request body data format |
| familiar | vtex checkout api put orders json payload |
| expert | checkout api put orders request body |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs checkout api update order request body data format", "style": "naive" },
  { "query": "vtex checkout api put orders json payload", "style": "familiar" },
  { "query": "checkout api put orders request body", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | what data format do i need to send when updating an order in vtex checkout api |
| familiar | what is the request body structure for updating orders in vtex checkout api |
| expert | how to format json payload for vtex checkout api put orders endpoint |

**Array (query_llm):**
```json
[
  { "query": "what data format do i need to send when updating an order in vtex checkout api", "style": "naive" },
  { "query": "what is the request body structure for updating orders in vtex checkout api", "style": "familiar" },
  { "query": "how to format json payload for vtex checkout api put orders endpoint", "style": "expert" }
]
```
