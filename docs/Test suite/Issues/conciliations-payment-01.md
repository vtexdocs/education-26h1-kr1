# Issue: Manage bank reconciliations for bank slip payments

| Field | Value |
|-------|--------|
| **issue_id** | conciliations-payment-01 |
| **persona** | Store operator |
| **product** | Payments |
| **user_intent** | How to manage bank reconciliations of payments made by bank slip. |
| **expected_doc_url** | https://help.vtex.com/docs/tutorials/bank-reconciliations |
| **source** | Command input (user issue) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how to reconcile bank slip payments in vtex |
| familiar | manage bank reconciliation for boleto payments vtex |
| expert | bank reconciliations vtex payments |

**Array (query_external):**
```json
[
  { "query": "how to reconcile bank slip payments in vtex", "style": "naive" },
  { "query": "manage bank reconciliation for boleto payments vtex", "style": "familiar" },
  { "query": "bank reconciliations vtex payments", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | bank reconciliation boleto |
| familiar | bank reconciliations |
| expert | bank reconciliations |

**Array (query_internal):**
```json
[
  { "query": "bank reconciliation boleto", "style": "naive" },
  { "query": "bank reconciliations", "style": "familiar" },
  { "query": "bank reconciliations", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs on bank reconciliations |
| familiar | vtex bank reconciliation for boleto payments |
| expert | bank reconciliations |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs on bank reconciliations", "style": "naive" },
  { "query": "vtex bank reconciliation for boleto payments", "style": "familiar" },
  { "query": "bank reconciliations", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how do i manage bank reconciliations for bank slip payments in vtex |
| familiar | steps to reconcile boleto payments in vtex |
| expert | how to manage bank reconciliations in vtex payments |

**Array (query_llm):**
```json
[
  { "query": "how do i manage bank reconciliations for bank slip payments in vtex", "style": "naive" },
  { "query": "steps to reconcile boleto payments in vtex", "style": "familiar" },
  { "query": "how to manage bank reconciliations in vtex payments", "style": "expert" }
]
```
