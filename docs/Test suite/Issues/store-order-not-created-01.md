# Issue: My store order was not created

| Field | Value |
|-------|--------|
| **issue_id** | store-order-not-created-01 |
| **persona** | Store operator |
| **product** | Orders / Post-purchase / Store operations |
| **user_intent** | Troubleshoot why orders are not being created in the store |
| **expected_doc_url** | https://help.vtex.com/en/troubleshooting/my-store-order-was-not-created |
| **source** | Command input (target document URL) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | VTEX order not created troubleshooting |
| familiar | VTEX store order creation error inventory |
| expert | my store order was not created VTEX |

**Array (query_external):**
```json
[
  { "query": "VTEX order not created troubleshooting", "style": "naive" },
  { "query": "VTEX store order creation error inventory", "style": "familiar" },
  { "query": "my store order was not created VTEX", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | order not created |
| familiar | order creation error inventory out of stock |
| expert | store order not created troubleshooting |

**Array (query_internal):**
```json
[
  { "query": "order not created", "style": "naive" },
  { "query": "order creation error inventory out of stock", "style": "familiar" },
  { "query": "store order not created troubleshooting", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs order not created troubleshooting |
| familiar | vtex store order creation error |
| expert | my store order was not created |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs order not created troubleshooting", "style": "naive" },
  { "query": "vtex store order creation error", "style": "familiar" },
  { "query": "my store order was not created", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | why aren't orders being created in my VTEX store |
| familiar | how to fix order creation errors in VTEX when inventory is out of stock |
| expert | my VTEX store order was not created what should I check |

**Array (query_llm):**
```json
[
  { "query": "why aren't orders being created in my VTEX store", "style": "naive" },
  { "query": "how to fix order creation errors in VTEX when inventory is out of stock", "style": "familiar" },
  { "query": "my VTEX store order was not created what should I check", "style": "expert" }
]
```
