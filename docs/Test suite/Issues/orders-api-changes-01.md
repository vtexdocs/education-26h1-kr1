# Issue: Get order changes/modifications history via API

| Field | Value |
|-------|--------|
| **issue_id** | orders-api-changes-01 |
| **persona** | Developer |
| **product** | Orders API / Order modifications |
| **user_intent** | How to get order changes/modifications history for an order via API. |
| **expected_doc_url** | https://developers.vtex.com/docs/api-reference/orders-api#get-/api/order-system/orders/-changeOrderId-/changes?endpoint=get-/api/order-system/orders/-changeOrderId-/changes |
| **source** | Command input (target document URL) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how to get order changes history in VTEX using the api |
| familiar | get order modifications history api vtex |
| expert | orders api get order changes vtex |

**Array (query_external):**
```json
[
  { "query": "how to get order changes history in VTEX using the api", "style": "naive" },
  { "query": "get order modifications history api vtex", "style": "familiar" },
  { "query": "orders api get order changes vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | order changes history api |
| familiar | get order modifications |
| expert | orders api changes |

**Array (query_internal):**
```json
[
  { "query": "order changes history api", "style": "naive" },
  { "query": "get order modifications", "style": "familiar" },
  { "query": "orders api changes", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs for getting order changes history via api |
| familiar | vtex orders api get changes |
| expert | orders api changes endpoint |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs for getting order changes history via api", "style": "naive" },
  { "query": "vtex orders api get changes", "style": "familiar" },
  { "query": "orders api changes endpoint", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how do i get the history of changes made to an order using vtex api |
| familiar | what api endpoint returns order modifications history in vtex |
| expert | how to get order changes via vtex orders api |

**Array (query_llm):**
```json
[
  { "query": "how do i get the history of changes made to an order using vtex api", "style": "naive" },
  { "query": "what api endpoint returns order modifications history in vtex", "style": "familiar" },
  { "query": "how to get order changes via vtex orders api", "style": "expert" }
]
```
