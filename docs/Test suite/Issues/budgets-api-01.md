# Issue: Create budgets via API

| Field | Value |
|-------|--------|
| **issue_id** | budgets-api-01 |
| **persona** | Developer |
| **product** | Budgets / B2B |
| **user_intent** | How to create budgets via API for B2B contexts. |
| **expected_doc_url** | https://developers.vtex.com/docs/api-reference/budgets-api#post-/api/budgets/-contextType-/-contextId-?endpoint=post-/api/budgets/-contextType-/-contextId- |
| **source** | Command input (target document URL) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how to create budgets in vtex using the api |
| familiar | create budget api vtex |
| expert | budgets api create budget vtex |

**Array (query_external):**
```json
[
  { "query": "how to create budgets in vtex using the api", "style": "naive" },
  { "query": "create budget api vtex", "style": "familiar" },
  { "query": "budgets api create budget vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | budget api create |
| familiar | create budget |
| expert | budgets api |

**Array (query_internal):**
```json
[
  { "query": "budget api create", "style": "naive" },
  { "query": "create budget", "style": "familiar" },
  { "query": "budgets api", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs for creating budgets via api |
| familiar | vtex budgets api create |
| expert | budgets api |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs for creating budgets via api", "style": "naive" },
  { "query": "vtex budgets api create", "style": "familiar" },
  { "query": "budgets api", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how do i create a budget using vtex api |
| familiar | what api endpoint creates budgets in vtex |
| expert | how to create budgets via vtex budgets api |

**Array (query_llm):**
```json
[
  { "query": "how do i create a budget using vtex api", "style": "naive" },
  { "query": "what api endpoint creates budgets in vtex", "style": "familiar" },
  { "query": "how to create budgets via vtex budgets api", "style": "expert" }
]
```
