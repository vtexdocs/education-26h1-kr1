# Issue: Implement a payment provider

| Field | Value |
|-------|--------|
| **issue_id** | payment-provider-02 |
| **persona** | Developer |
| **product** | Payments |
| **user_intent** | Learn how a payment provider should be developed to process on VTEX. |
| **expected_doc_url** | https://developers.vtex.com/docs/guides/payments-integration-implementing-a-payment-provider |
| **source** | Command input (user issue) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how do i build a payment provider for vtex |
| familiar | develop a payment provider for vtex payments |
| expert | payments integration implementing a payment provider vtex |

**Array (query_external):**
```json
[
  { "query": "how do i build a payment provider for vtex", "style": "naive" },
  { "query": "develop a payment provider for vtex payments", "style": "familiar" },
  { "query": "payments integration implementing a payment provider vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | build payment provider vtex |
| familiar | payment provider implementation |
| expert | implementing a payment provider |

**Array (query_internal):**
```json
[
  { "query": "build payment provider vtex", "style": "naive" },
  { "query": "payment provider implementation", "style": "familiar" },
  { "query": "implementing a payment provider", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs on building a payment provider |
| familiar | vtex payment provider implementation guide |
| expert | payments integration implementing a payment provider |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs on building a payment provider", "style": "naive" },
  { "query": "vtex payment provider implementation guide", "style": "familiar" },
  { "query": "payments integration implementing a payment provider", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how do i develop a payment provider that works with vtex |
| familiar | what are the steps to implement a payment provider in vtex |
| expert | how to implement a payment provider for vtex payments integration |

**Array (query_llm):**
```json
[
  { "query": "how do i develop a payment provider that works with vtex", "style": "naive" },
  { "query": "what are the steps to implement a payment provider in vtex", "style": "familiar" },
  { "query": "how to implement a payment provider for vtex payments integration", "style": "expert" }
]
```
