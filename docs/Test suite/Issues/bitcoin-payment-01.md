# Issue: Enable Bitcoin payments

| Field | Value |
|-------|--------|
| **issue_id** | bitcoin-payment-01 |
| **persona** | Store operator |
| **product** | Payments |
| **user_intent** | How to configure bitcoin as a payment method in my VTEX store. |
| **expected_doc_url** | https://help.vtex.com/docs/tutorials/enabling-payments-with-bitcoin |
| **source** | Command input (user issue) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how to enable bitcoin payments in my vtex store |
| familiar | configure bitcoin payment method vtex |
| expert | enabling payments with bitcoin vtex |

**Array (query_external):**
```json
[
  { "query": "how to enable bitcoin payments in my vtex store", "style": "naive" },
  { "query": "configure bitcoin payment method vtex", "style": "familiar" },
  { "query": "enabling payments with bitcoin vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | bitcoin payment vtex |
| familiar | enable bitcoin payments |
| expert | enabling payments with bitcoin |

**Array (query_internal):**
```json
[
  { "query": "bitcoin payment vtex", "style": "naive" },
  { "query": "enable bitcoin payments", "style": "familiar" },
  { "query": "enabling payments with bitcoin", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs on enabling bitcoin payments |
| familiar | vtex bitcoin payment method setup |
| expert | enabling payments with bitcoin |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs on enabling bitcoin payments", "style": "naive" },
  { "query": "vtex bitcoin payment method setup", "style": "familiar" },
  { "query": "enabling payments with bitcoin", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how do i set up bitcoin payments in my vtex store |
| familiar | steps to configure bitcoin as a payment method in vtex |
| expert | how to enable payments with bitcoin in vtex |

**Array (query_llm):**
```json
[
  { "query": "how do i set up bitcoin payments in my vtex store", "style": "naive" },
  { "query": "steps to configure bitcoin as a payment method in vtex", "style": "familiar" },
  { "query": "how to enable payments with bitcoin in vtex", "style": "expert" }
]
```
