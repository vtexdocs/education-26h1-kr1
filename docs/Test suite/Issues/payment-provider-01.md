# Issue: Integrate a payment provider

| Field | Value |
|-------|--------|
| **issue_id** | payment-provider-01 |
| **persona** | Developer |
| **product** | Payments |
| **user_intent** | Understand how to integrate a payment provider into VTEX. |
| **expected_doc_url** | https://developers.vtex.com/docs/guides/integrating-a-new-payment-provider-on-vtex |
| **source** | Command input (user issue) |

---

## Queries by path

### A — External search (Google)

| style | query |
|-------|--------|
| naive | how do i add a new payment provider to vtex |
| familiar | integrate payment provider vtex platform |
| expert | integrating a new payment provider on vtex |

**Array (query_external):**
```json
[
  { "query": "how do i add a new payment provider to vtex", "style": "naive" },
  { "query": "integrate payment provider vtex platform", "style": "familiar" },
  { "query": "integrating a new payment provider on vtex", "style": "expert" }
]
```

### B — Internal search (Algolia / Proprietary API)

| style | query |
|-------|--------|
| naive | add payment provider vtex |
| familiar | payment provider integration |
| expert | integrating a new payment provider on vtex |

**Array (query_internal):**
```json
[
  { "query": "add payment provider vtex", "style": "naive" },
  { "query": "payment provider integration", "style": "familiar" },
  { "query": "integrating a new payment provider on vtex", "style": "expert" }
]
```

### C — MCP (proprietary docs)

| style | query |
|-------|--------|
| naive | vtex docs on adding a payment provider |
| familiar | vtex payment provider integration guide |
| expert | integrating a new payment provider on vtex |

**Array (query_mcp):**
```json
[
  { "query": "vtex docs on adding a payment provider", "style": "naive" },
  { "query": "vtex payment provider integration guide", "style": "familiar" },
  { "query": "integrating a new payment provider on vtex", "style": "expert" }
]
```

### D — External LLMs

| style | query |
|-------|--------|
| naive | how can i integrate my payment provider with vtex |
| familiar | steps to integrate a payment provider in vtex |
| expert | how to integrate a new payment provider on vtex |

**Array (query_llm):**
```json
[
  { "query": "how can i integrate my payment provider with vtex", "style": "naive" },
  { "query": "steps to integrate a payment provider in vtex", "style": "familiar" },
  { "query": "how to integrate a new payment provider on vtex", "style": "expert" }
]
```
